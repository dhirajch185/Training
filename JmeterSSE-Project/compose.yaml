services:
  sse:
    build: .
    image: sse-server:latest
    ports:
      - "8081:8080"
    # environment:
    #   - LLM_PROVIDER=mock        # or gemini (needs GEMINI_API_KEY)
    #   - MOCK_DELAY_MS=500
